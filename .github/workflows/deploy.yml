name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3 # Atualize para a versão v3

      - name: Set up Node.js
        uses: actions/setup-node@v3 # Atualize para a versão v3
        with:
          node-version: "18"

      - name: Install dependencies and build website
        run: |
          cd ../var/www/Bobagi-Website/website
          npm install
          npm run build

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            cd ../var/www/Bobagi-Website/
            git pull origin main
            cd website
            npm install
            npm run build
            cd ../server
            npm install
            pm2 restart all
